{
  "name": "react-peer",
  "repository": "git@github.com:cerc-io/react-peer.git",
  "license": "AGPL-3.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.4.1"
  },
  "scripts": {
    "build": "lerna run build --stream",
    "publish:workspace": "yarn build && lerna publish",
    "version:set": "lerna version --no-git-tag-version"
  }
}
